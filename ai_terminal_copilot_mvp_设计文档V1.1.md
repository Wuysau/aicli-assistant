# Windows Terminal Workflow Assistant MVP 设计文档（重构版）

## 1. 重构原因

原始版本把产品定义为一个较泛化的“AI Terminal Copilot”。这个方向虽然容易理解，但与以下产品的能力边界重叠较大：

- Codex / Claude Code 一类通用 coding agent
- Warp 一类带 agent 能力的终端产品
- IDE 内置的 AI 助手

如果继续沿着“通用命令生成 + 通用报错分析 + 通用开发辅助”推进，产品会很容易沦为现有大模型代理工具的子集，缺乏独立存在价值。

因此本次重构的核心目标是：

**从“通用 AI 终端助手”收缩为“面向 Windows 开发与运维场景的终端工作流助手”。**

---

## 2. 新产品定位

### 2.1 产品名称（暂定）
Windows Terminal Workflow Assistant

可选名称：
- WinOps Terminal Assistant
- PowerShell & WSL Workflow Assistant
- Windows 开发排障助手
- Windows Command Desk

### 2.2 一句话定义
一个运行在 Windows 上、服务于开发者和运维人员的终端工作流助手，专门帮助用户在 **PowerShell / cmd / WSL / SSH** 场景下更快完成高频任务、定位常见问题、规避危险操作，并沉淀可复用的操作流程。

### 2.3 与通用 Coding Agent 的差异
本产品不是为了替代 Codex / Claude Code，而是聚焦它们不擅长产品化覆盖的部分：

1. **更懂 Windows 终端差异**
2. **更懂 PowerShell / cmd / Bash 三套命令语义差异**
3. **更懂开发/运维高频排障流程**
4. **更强调风险提醒、模板复用和 SOP 沉淀**
5. **更适合作为日常工具，而不是复杂通用代理**

---

## 3. 新目标用户

### 3.1 核心用户画像
MVP 首批用户不是“所有程序员”，而是以下更具体的人群：

#### 用户 A：Windows 为主的后端开发者
特征：
- 日常使用 Windows Terminal / PowerShell / cmd
- 同时可能使用 WSL
- 经常做 Git、Maven、Java 启动、日志排查、端口排查
- 需要 SSH 到 Linux 机器上排查问题

#### 用户 B：Windows 桌面环境下的运维/实施/支持工程师
特征：
- 经常在 Windows 本机与 Linux 服务器之间切换
- 命令记忆负担大
- 常见任务重复率高
- 风险操作多，误操作成本高

#### 用户 C：命令行能力一般的初中级开发者
特征：
- 知道要做什么，但写不准命令
- 报错能看出不对，但不知道如何收缩定位范围
- 需要“解释 + 下一步建议 + 风险提醒”三件套

### 3.2 明确不优先服务的人群
本期不优先服务：
- 专业 Linux 终端重度玩家
- 追求全自动 agent 的高级开发者
- 大规模企业协作/权限管理场景
- 通用 AI 编码代理使用者

---

## 4. 核心问题定义

重构后，产品不再试图回答“任何终端问题”，只聚焦以下四类高频问题。

### 问题 1：同一个意图在不同 shell 下怎么写
例如：
- 查端口占用
- 查日志包含某关键字的行数
- 删除文件
- 查看环境变量
- 进入某目录并执行命令

用户真正需要的是：
- PowerShell 写法
- cmd 写法
- Bash/WSL 写法
- 它们的差异解释

### 问题 2：Windows + WSL + SSH 混合环境下容易混乱
例如：
- 路径是 `C:\...` 还是 `/mnt/c/...`
- 当前命令应该在 PowerShell 执行还是在 WSL 执行
- 本机端口占用排查和 Linux 机器排查命令不同
- 文件权限和环境变量表现不一致

### 问题 3：开发/运维高频排障工作流重复且割裂
例如：
- Java 服务起不来
- 端口被占用
- Maven 打包失败
- Git push 被 hook 拒绝
- SSH 连不上
- 服务状态不对
- 日志里出现 500 / ERROR / timeout

### 问题 4：危险命令缺乏明确提醒
例如：
- 删除命令
- 覆盖输出
- Git 强推
- 生产机器执行操作
- Docker 清理类命令

---

## 5. 新价值主张

### 5.1 产品核心价值
本产品不是“更聪明的通用 agent”，而是“更稳、更顺手的 Windows 终端工作流工具”。

它要给用户的价值变成：

1. **跨 shell 翻译**：同一个任务给出 PowerShell / cmd / Bash 的正确写法
2. **排障闭环**：遇到常见错误时，给出 Windows 场景下更实际的定位路径
3. **风险控制**：在执行前就指出危险点
4. **流程沉淀**：把高频任务保存为模板和 SOP

### 5.2 MVP 卖点改写
重构后，MVP 不再宣传：
- AI 帮你做任何终端任务

而是宣传：
- 帮你在 Windows 上少查命令
- 帮你看懂 PowerShell / cmd / WSL 差异
- 帮你更快排掉开发和运维常见问题
- 帮你少踩危险命令的坑

---

## 6. MVP 功能范围（重构版）

## 6.1 Must Have 功能

### 功能 A：跨 Shell 命令生成

#### 功能描述
用户输入一个意图，系统输出该意图在不同环境下的命令写法。

#### 支持环境
- PowerShell
- cmd
- Bash / WSL

#### 示例输入
- 查看 8103 端口被谁占用
- 统计 application.log 中包含 ERROR 的行数
- 查看某个环境变量
- 找出当前目录下所有 `.log` 文件
- SSH 登录 10.0.0.15

#### 输出结构
- 推荐环境
- PowerShell 命令
- cmd 命令
- Bash/WSL 命令
- 差异说明
- 适用场景说明

#### 新价值
这是通用 coding agent 通常不会专门做成产品化主路径的能力。

---

### 功能 B：Windows 场景报错拆解

#### 功能描述
用户粘贴报错后，系统根据当前环境给出更贴近 Windows 工作流的解释和下一步建议。

#### 首批支持场景
- Git 错误
- Maven / Gradle 构建错误
- Java 启动错误
- 端口占用
- PowerShell 执行策略问题
- SSH 连接错误
- Docker 常见错误
- Linux 服务状态排查入口

#### 输出内容
- 错误摘要
- 可能原因
- 建议排查顺序
- 可复制命令
- 当前问题更适合在哪个环境继续操作

#### 与旧版差异
不做“泛化错误分析”，而是强调“当前错误应该去 PowerShell、cmd、WSL 还是 SSH 后的 Linux 环境处理”。

---

### 功能 C：命令环境判断

#### 功能描述
用户输入一条命令或一个任务，系统告诉用户：
- 这条命令适合在哪个环境执行
- 是否支持 PowerShell / cmd / Bash
- 如果放错环境，可能会出现什么问题

#### 示例
输入：
- `grep "ERROR" application.log | wc -l`

输出：
- 更适合 Bash/WSL
- Windows PowerShell 下可改写为 `Select-String ...`
- cmd 下实现较差，不推荐

#### 价值
这是旧版缺失但很关键的能力，它能直接解决“为什么别人给我的命令跑不通”。

---

### 功能 D：高频排障工作流模板

#### 功能描述
首版内置少量高频模板，不依赖用户先自己收藏。

#### 建议首批模板
- 端口占用排查
- Java 服务启动失败排查
- Maven 打包失败基础排查
- Git push / hook 拒绝排查
- SSH 连不上排查
- 查看日志中 ERROR / WARN / 500

#### 模板内容
- 问题目标
- 推荐环境
- 分步命令
- 每一步说明
- 风险提醒

#### 与旧版差异
旧版偏“用户自己收藏”；新版先做“系统内置专家模板”，更容易立即体现价值。

---

### 功能 E：危险命令识别

#### 功能描述
系统对高风险命令做识别，并给出更贴近 Windows / 服务器场景的提醒。

#### 首批风险类别
- 删除文件 / 目录
- Git 强制推送 / reset
- 覆盖重定向
- Docker prune / rm
- Linux root 级删除命令
- 疑似生产环境操作

#### 输出内容
- 风险等级
- 影响范围
- 是否可逆
- 更稳妥替代方案

---

## 6.2 Should Have 功能

### 功能 F：命令对照解释
把一条命令在不同 shell 下的等价写法放在一起对照展示。

### 功能 G：最近问题与最近模板
保留最近查询和最近用过的工作流。

### 功能 H：收藏为个人模板
允许用户把结果保存成自己的快捷模板。

### 功能 I：问题入口分类
让用户一开始选择：
- 我想做什么
- 我这个报错怎么处理
- 这条命令应该在哪跑
- 我想按模板排查

---

## 6.3 Won’t Have
本期明确不做：
- 完整终端模拟器
- 自动执行命令
- 代码库级 agent 能力
- 自动修改工程文件
- 自动创建 PR / Git 工作流
- 通用编程助手
- 团队协作后台

---

## 7. 页面结构（重构版）

## 7.1 页面原则
首页必须让用户一眼看到“这是个终端工作流工具，不是聊天机器人”。

### 页面主结构
1. 任务类型选择区
2. 主输入区
3. 环境选择区
4. 结果区
5. 内置模板区
6. 最近记录区

---

## 7.2 首页任务入口
首页不再只给一个通用输入框，而是给四个清晰入口：

- **生成命令**：我想完成一个任务
- **分析报错**：我遇到了一个错误
- **判断环境**：我不知道这条命令该在哪跑
- **使用模板**：我想按固定流程排查

这一步非常重要，因为它会让产品和通用 AI 对话工具产生明显体验差异。

---

## 7.3 结果页结构
结果页建议包括：

- 推荐操作环境
- 主推荐命令
- 其他环境下的等价写法
- 差异解释
- 风险提醒
- 下一步建议
- 收藏模板按钮

如果是报错分析，还要额外包括：
- 排查步骤 1 / 2 / 3
- 哪一步在本机做
- 哪一步在 Linux 机器做

---

## 8. 数据模型（重构版）

### 8.1 QueryRecord
字段建议：
- id
- query_type
- user_input
- preferred_shell
- recommended_environment
- result_json
- risk_level
- created_at

### 8.2 BuiltInWorkflow
字段建议：
- id
- name
- category
- recommended_environment
- steps_json
- tags
- created_at

### 8.3 UserTemplate
字段建议：
- id
- name
- description
- preferred_shell
- command_or_steps_json
- created_at
- updated_at

### 8.4 ErrorPattern
字段建议：
- id
- category
- raw_text
- summary
- suggested_checks_json
- recommended_environment
- created_at

---

## 9. 技术实现重点变化

## 9.1 AI 层不再追求通用问答
AI 层要围绕固定问题类型设计：

1. 任务转命令
2. 报错转排查步骤
3. 命令转环境判断
4. 模板转结构化流程

### 这样做的好处
- 输出更稳定
- 更适合规则约束
- 更适合后续沉淀高频场景模板
- 更不容易和通用 agent 直接撞车

---

## 9.2 规则层权重提高
重构后要提高规则引擎比重，不能全靠模型自由发挥。

### 规则层适用场景
- shell 环境判断
- 高风险命令识别
- 常见错误关键词分类
- 内置模板匹配

### AI 层适用场景
- 解释差异
- 组织排查步骤
- 生成自然语言说明
- 输出更友好的建议

---

## 9.3 内置知识优先于自由生成
新版 MVP 应优先内置一批“高频命令知识”和“排障模板”。

原因：
- 高频问题稳定
- 更容易验证正确性
- 比完全依赖模型更靠谱
- 更适合形成产品护城河

---

## 10. 首批内置场景建议

重构后，MVP 不要什么都碰，只做 10~15 个高频场景。

### 建议首批场景
1. 查端口占用
2. 杀掉占用端口的进程
3. 查看日志中 ERROR / WARN / 500
4. 统计日志关键字次数
5. Maven 打包并跳过测试
6. Git 查看当前提交者信息
7. Git push 被 hook 拒绝排查
8. Java 端口冲突排查
9. PowerShell 执行策略错误
10. SSH 连接失败基础排查
11. Docker 查看容器状态
12. systemctl 查看服务状态
13. 查找某类文件
14. 查看环境变量
15. 路径格式转换（Windows 路径与 WSL 路径）

---

## 11. MVP 开发顺序（重构版）

## 第一阶段：做出差异化最小闭环
只做四个核心能力：

1. 跨 shell 命令生成
2. 环境判断
3. 报错拆解
4. 内置模板展示

### 交付标准
- 至少覆盖 10 个高频场景
- 每个场景都能输出 PowerShell / cmd / Bash 结果或明确说明不适用
- 每个结果都带环境建议

---

## 第二阶段：补风险与复用
增加：
- 危险命令识别
- 收藏为个人模板
- 最近问题记录
- 模板搜索

---

## 第三阶段：增强工作流价值
增加：
- 语义搜索历史
- 更丰富的模板库
- SSH / 本机 / WSL 环境切换辅助
- 结果一键复制到剪贴板

---

## 12. 验收标准（重构版）

满足以下条件即可认为新版 MVP 达标：

1. 用户能输入一个任务并看到不同 shell 下的命令写法
2. 系统能明确告诉用户“应该在哪个环境执行”
3. 用户粘贴错误后，系统能给出 Windows 场景下可执行的排查顺序
4. 用户能直接使用内置模板处理常见问题
5. 对危险命令能给出有效提醒
6. 用户能在不依赖通用 coding agent 的情况下，完成常见终端任务闭环

---

## 13. 商业化方向（重构版）

重构后，商业化切入点也要更具体。

### 方向 A：个人效率工具
面向 Windows 开发者 / 运维人员，卖点是：
- 更快查命令
- 更快排问题
- 更少犯错

### 方向 B：团队 SOP 工具
把团队常用命令和排障步骤沉淀成模板库。

### 方向 C：企业内部 Windows 开发支持工具
用于支持 PowerShell / WSL / SSH 混合环境下的标准化操作。

---

## 14. 最终结论

重构后的产品，不再是一个容易被 Codex / Claude Code 覆盖的“通用 AI 终端助手”。

它的新定义是：

**一个面向 Windows 开发与运维场景的终端工作流工具。**

它的竞争策略不是：
- 比通用 agent 更聪明

而是：
- 比通用 agent 更懂 Windows 终端差异
- 比通用 agent 更懂高频排障流程
- 比通用 agent 更适合日常重复使用
- 比通用 agent 更强调风险控制和模板沉淀

如果后续继续推进，开发重点也应始终围绕这四件事：

1. 环境判断
2. 差异翻译
3. 排障模板
4. 风险控制

这四项才是这个项目在今天仍然有独立价值的真正基础。

