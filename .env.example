VITE_APP_NAME=AI CLI Assistant
VITE_APP_ENV=local
VITE_MOCK_DELAY_MS=450

# AI providers are configured inside the desktop app settings page
# and stored in the local application directory instead of this file.
