# AI Terminal Copilot MVP 设计文档

## 1. 项目定位

### 1.1 产品名称（暂定）
AI Terminal Copilot

### 1.2 产品一句话定义
一个运行在 Windows 上、挂在现有终端旁边的 AI 助手，帮助开发者和运维人员更快地生成命令、理解报错、复用工作流，并降低高风险操作带来的失误成本。

### 1.3 MVP 阶段核心目标
MVP 不追求“替代 Windows Terminal / PowerShell / cmd / WSL”，而是追求：

1. 让用户少查资料
2. 让用户少写命令
3. 让用户少踩坑
4. 让用户把常见排障和操作过程沉淀下来

### 1.4 不做什么
MVP 明确不做以下内容：

- 不自研完整终端模拟器
- 不自研 GPU 文本渲染
- 不做 tab / split / terminal shell 宿主
- 不做复杂插件生态
- 不做多人协作
- 不做企业级权限系统
- 不做自动执行高风险运维脚本

---

## 2. 目标用户

### 2.1 第一批目标用户
优先服务以下用户：

- Windows 开发者
- 使用 PowerShell / cmd / WSL 的程序员
- 经常 SSH 登录 Linux 机器的人
- Java / Spring Boot 开发者
- 常做 Git、Maven、Docker、日志排查、服务器排障的人

### 2.2 用户典型痛点

#### 痛点 1：命令记不住
用户知道“要做什么”，但不知道命令怎么写。

例如：
- 查某个端口被谁占用
- 统计日志里某类报错出现次数
- 用 Maven 打包并跳过测试
- 查看 systemd 服务状态
- Git 回退、rebase、cherry-pick

#### 痛点 2：报错看不懂
用户看到一大段错误输出，但不知道根因在哪。

例如：
- Maven 依赖下载失败
- Git push 被 hook 拒绝
- SSH 连接失败
- Docker 启动失败
- PowerShell 权限问题
- Java 启动端口冲突

#### 痛点 3：重复劳动多
很多常见任务会反复做，但每次都要重新想命令。

例如：
- 启动本地服务
- 看指定日志
- SSH 到固定机器
- grep 某类关键字
- 打包发布前检查

#### 痛点 4：高风险操作容易误伤
例如：
- `git push --force`
- 删除目录
- 覆盖配置
- 生产机器执行危险命令

---

## 3. 产品价值主张

### 3.1 对用户的价值
这个产品要让用户获得 4 个明确收益：

1. **命令生成更快**：把自然语言转成可执行命令
2. **排障更快**：读懂报错，给出原因和下一步建议
3. **经验可复用**：把一次解决过程沉淀为模板
4. **操作更安全**：识别危险命令并提醒

### 3.2 MVP 的核心卖点
MVP 阶段只强调三个卖点：

- 你不用背命令
- 你不用硬啃报错
- 你不用每次重复造轮子

---

## 4. 产品形态

### 4.1 整体形态
产品形态为一个独立的 Windows 桌面应用，不替代终端，而是作为“终端旁的智能助手”存在。

### 4.2 与现有终端的关系
用户继续使用自己熟悉的终端：

- Windows Terminal
- PowerShell
- cmd
- WSL
- SSH
- 也可以兼容 VS Code 内置终端

本产品负责：

- 接收用户自然语言需求
- 读取用户粘贴的报错输出
- 生成建议命令
- 解释命令含义
- 给出风险提示
- 保存常用工作流

### 4.3 MVP 推荐交互方式
MVP 采用最稳妥的交互方式：

- 用户在 AI 助手窗口输入需求
- 系统输出建议命令
- 用户点击“复制命令”
- 用户手动粘贴到终端执行

这样做的原因：

- 技术风险低
- 不需要深度绑定不同终端
- 安全性更高
- 更容易让用户建立信任

MVP 后续可增加“注入到活动终端输入框”的增强能力，但不作为首发强依赖。

---

## 5. MVP 功能范围

## 5.1 功能优先级说明
采用 MoSCoW 思路：

- Must have：没有这些就不算 MVP
- Should have：能明显提升体验，但首版可以简化
- Could have：后续增强
- Won’t have：本阶段不做

---

## 5.2 Must Have 功能

### 功能 A：自然语言生成命令

#### 功能描述
用户输入自然语言需求，系统生成适用于目标环境的命令。

#### 示例输入
- 查看 8103 端口被谁占用
- 用 Maven 打包并跳过测试
- 查 application.log 中包含 ERROR 的行数
- SSH 到 10.0.0.15 服务器
- 查看 Docker 容器状态

#### 输出内容
- 推荐命令
- 命令适用环境（PowerShell / cmd / Bash）
- 命令解释
- 可选替代方案

#### MVP 要求
- 支持 PowerShell
- 支持 cmd
- 支持 Bash/WSL
- 支持基础命令解释

---

### 功能 B：报错解释

#### 功能描述
用户粘贴错误输出，系统分析错误类型、可能原因和建议动作。

#### 支持的首批错误类别
- Git 常见错误
- Maven 常见错误
- Java 启动错误
- PowerShell 权限错误
- SSH 连接错误
- Docker 常见错误
- Linux 服务状态类错误

#### 输出内容
- 错误摘要
- 可能根因（按概率排序）
- 建议检查项
- 可复制命令

#### MVP 要求
- 必须支持用户手动粘贴文本
- 不依赖自动抓取终端输出

---

### 功能 C：命令解释

#### 功能描述
用户输入一条命令，系统用自然语言解释其作用。

#### 示例
输入：
`mvn clean package -Dmaven.test.skip=true`

输出：
- `clean` 的作用
- `package` 的作用
- `-D...` 参数的作用
- 可能副作用

#### MVP 价值
这项能力对初中级开发者非常有用，可以直接降低学习门槛。

---

### 功能 D：命令历史与最近会话

#### 功能描述
保存用户最近查询过的问题、生成过的命令和分析过的报错。

#### MVP 要求
- 本地存储
- 支持最近记录列表
- 支持点击再次打开

#### 价值
- 减少重复输入
- 形成初步“工作记忆”

---

### 功能 E：一键复制

#### 功能描述
对所有生成的命令提供一键复制。

#### 为什么是 Must Have
MVP 阶段不强求自动执行，因此复制能力是核心闭环的一部分。

---

## 5.3 Should Have 功能

### 功能 F：风险提示

#### 功能描述
当系统识别到潜在危险命令时，显示风险级别与提醒。

#### 首批高风险类别
- 删除文件/目录
- 强制 Git 操作
- 覆盖性重定向
- 可疑生产环境操作
- Docker 删除类命令

#### 风险等级
- 低：常规命令
- 中：可能影响当前目录/当前仓库
- 高：可能造成不可逆后果

---

### 功能 G：模板 / 快捷工作流

#### 功能描述
允许用户保存常见任务为模板。

#### 示例
- 本地 Spring Boot 打包并启动
- 查看指定日志并过滤 ERROR
- Git 提交前检查
- SSH 登录某台服务器

#### MVP 简化做法
- 只支持手动收藏为模板
- 每个模板包含名称、说明、命令内容、适用环境

---

### 功能 H：环境切换

#### 功能描述
同一个需求在不同 shell 下命令写法不同，系统允许用户切换输出环境。

#### 支持环境
- PowerShell
- cmd
- Bash/WSL

---

## 5.4 Could Have 功能

### 功能 I：自动识别项目类型
根据当前目录或用户选择，推断项目为：
- Maven 项目
- Gradle 项目
- Node 项目
- Python 项目

### 功能 J：SSH 机器收藏
用户保存常用机器地址、登录名、标签。

### 功能 K：语义搜索历史
支持通过自然语言搜索过去记录：
- 上次统计日志报错次数的命令
- 上次打包 Java 项目的命令

### 功能 L：一键注入终端
将命令直接输入到当前活动终端，不自动回车执行。

---

## 5.5 Won’t Have（本期不做）

- 完整终端模拟器
- 命令自动执行闭环
- 团队协作空间
- 云端同步
- 企业 SSO / 权限管理
- 插件市场
- 多模型路由系统
- 自动读取全部本机 shell 历史
- 自动扫描全盘项目

---

## 6. 核心用户流程

## 6.1 流程一：生成命令
1. 用户打开应用
2. 输入自然语言需求
3. 选择目标 shell
4. 系统生成命令
5. 系统展示命令解释和风险提示
6. 用户点击复制
7. 用户到终端执行

## 6.2 流程二：分析报错
1. 用户复制终端中的错误输出
2. 粘贴到应用中
3. 系统识别错误类型
4. 输出根因分析
5. 输出建议命令
6. 用户复制并去终端验证

## 6.3 流程三：收藏模板
1. 用户对一个满意的命令结果点击收藏
2. 输入模板名称
3. 下次从模板中快速复用

---

## 7. 页面与信息架构

## 7.1 页面结构
MVP 建议只有 4 个主页面：

1. 首页 / 问题输入页
2. 结果详情页
3. 历史记录页
4. 模板页

---

## 7.2 首页

### 区块 1：输入框
支持两种输入模式：
- 我要做什么
- 我遇到了什么错误

### 区块 2：环境选择器
- PowerShell
- cmd
- Bash/WSL

### 区块 3：快捷入口
- 生成命令
- 解释命令
- 分析报错

### 区块 4：最近使用
展示最近 5 条记录

---

## 7.3 结果详情页

### 展示内容
- 输出标题
- 建议命令
- 复制按钮
- 风险级别
- 命令解释
- 备选命令
- 注意事项
- 收藏为模板按钮

---

## 7.4 历史记录页

### 列表项字段
- 类型（生成命令 / 报错分析 / 命令解释）
- 标题
- 创建时间
- shell 环境
- 是否已收藏为模板

---

## 7.5 模板页

### 列表项字段
- 模板名称
- 命令内容
- 适用环境
- 描述
- 使用次数

---

## 8. 数据模型（MVP 级）

## 8.1 QueryRecord
用于保存每次用户查询记录。

字段建议：
- id
- type
- user_input
- shell_type
- result_text
- risk_level
- created_at
- favorite_template_id（可空）

## 8.2 Template
用于保存用户收藏的模板。

字段建议：
- id
- name
- description
- shell_type
- command_text
- tags
- created_at
- updated_at

## 8.3 ErrorAnalysis
用于保存报错分析结果。

字段建议：
- id
- raw_error_text
- summary
- possible_causes
- suggested_actions
- created_at

---

## 9. 系统架构设计

## 9.1 架构原则
MVP 阶段遵循以下原则：

- 架构简单
- 本地优先
- 模块边界清晰
- 易于后续演进

## 9.2 建议架构分层

### 1）桌面端 UI 层
负责：
- 输入与展示
- 历史记录
- 模板管理
- 复制操作

### 2）本地应用服务层
负责：
- Prompt 组装
- 结果解析
- 风险识别
- 模板逻辑
- 数据持久化

### 3）AI 能力层
负责：
- 命令生成
- 报错分析
- 命令解释

### 4）本地存储层
负责：
- 历史记录保存
- 模板保存
- 用户偏好保存

---

## 10. 技术选型建议

## 10.1 桌面端
优先建议：

### 方案 A：Tauri + React + TypeScript
优点：
- 轻量
- Windows 体验较好
- 前端 UI 开发效率高
- 对单人开发友好

缺点：
- 后续如果做系统级深度集成，要补更多原生能力

### 方案 B：Electron + React
优点：
- 生态成熟
- 上手容易
- 大量现成方案

缺点：
- 体积偏大
- 资源占用较高

### 推荐结论
**MVP 推荐 Tauri + React + TypeScript。**

---

## 10.2 本地数据库
推荐：SQLite

原因：
- 轻量
- 易部署
- 适合历史记录和模板存储

---

## 10.3 AI 接口层
MVP 建议先接单一模型服务，不要一开始做多模型抽象过度设计。

接口层职责：
- 标准化输入输出
- 控制 token 长度
- 记录调用日志
- 错误重试

---

## 10.4 命令风险识别
MVP 阶段建议使用两层识别：

### 第一层：规则引擎
通过关键词规则先做快速识别。

例如：
- `rm -rf`
- `git push --force`
- `del /f /s /q`
- `docker system prune`

### 第二层：AI 补充判断
对复杂语义进行补充解释。

---

## 10.5 终端集成策略
MVP 不直接执行命令，只做：
- 复制到剪贴板
- 可选地“粘贴到活动窗口”作为后续版本探索

这样能显著降低系统兼容复杂度。

---

## 11. AI 能力设计

## 11.1 AI 输入分类
MVP 将用户输入分为三类：

1. 命令生成请求
2. 报错分析请求
3. 命令解释请求

系统先做意图识别，再走不同 prompt 模板。

---

## 11.2 输出格式约束
为了便于 UI 展示，AI 返回应结构化。

建议统一结构：
- intent_type
- shell_type
- primary_command
- alternate_commands
- explanation
- risk_level
- cautions
- follow_up_questions（MVP 可为空）

---

## 11.3 Prompt 设计原则

### 原则 1：优先输出可执行命令
避免泛泛而谈。

### 原则 2：明确 shell 上下文
PowerShell、cmd、Bash 的命令差异要区分。

### 原则 3：对风险命令必须解释
不能只给命令，不给提醒。

### 原则 4：优先给最短闭环答案
用户大多数场景下要的是“下一步执行什么”。

---

## 12. 非功能需求

## 12.1 性能
- 单次普通请求响应尽量控制在可接受范围内
- 历史和模板页面应秒开

## 12.2 可用性
- 新用户首次打开即可使用
- 关键路径不超过 2 次点击

## 12.3 安全性
- 默认不自动执行命令
- 本地敏感内容不上传，除非用户显式发送
- 对明显高风险命令做前置提醒

## 12.4 可维护性
- 模块清晰
- 便于后续接入更多 shell 和场景

---

## 13. MVP 开发顺序

## 第一阶段：最小闭环
目标：先打通“输入 -> AI 输出 -> 复制 -> 历史保存”

开发内容：
- 首页输入框
- shell 选择器
- 命令生成
- 命令解释
- 报错分析
- 结果页
- 一键复制
- 历史记录

## 第二阶段：体验增强
目标：让产品更像一个可持续使用的工具

开发内容：
- 模板收藏
- 风险提示
- 历史搜索
- 结果展示优化

## 第三阶段：差异化增强
目标：开始形成自己的产品壁垒

开发内容：
- 语义搜索历史
- SSH 收藏
- 项目类型识别
- 一键注入终端（不自动执行）

---

## 14. 验收标准（MVP）

满足以下条件，即可视为 MVP 达标：

1. 用户能输入自然语言并得到正确方向的命令
2. 用户能粘贴错误并得到有帮助的分析
3. 用户能看懂生成命令的含义
4. 用户能复制结果并在终端执行
5. 用户能在历史中找到之前的记录
6. 用户能收藏至少一种模板
7. 对明显危险命令能给出提醒

---

## 15. 商业化初步方案

## 15.1 免费版
提供：
- 每日有限次数查询
- 基础命令生成
- 基础报错分析
- 历史记录

## 15.2 付费版
提供：
- 更高调用额度
- 模板功能
- 风险分析增强
- 历史语义搜索
- SSH 收藏
- 项目上下文增强

## 15.3 更现实的早期收费方式
MVP 初期不一定要马上做在线支付。更现实的方式是：
- 先做可试用版本
- 找真实用户试用
- 再决定是订阅制还是一次性买断制

---

## 16. 风险与应对

## 风险 1：AI 输出不稳定
应对：
- 增加结构化输出约束
- 增加规则兜底
- 为高频场景建立模板库

## 风险 2：不同 shell 语法差异大
应对：
- 强制要求用户选择 shell
- 为每个 shell 维护独立 prompt 约束

## 风险 3：用户不信任自动执行
应对：
- MVP 不自动执行
- 强化解释和风险提示

## 风险 4：场景过多导致范围失控
应对：
- 先聚焦开发者常见命令
- 优先做 Git / Maven / Java / SSH / 日志 / Docker 场景

---

## 17. 版本路线图建议

## V0.1
- 自然语言生成命令
- 报错分析
- 命令解释
- 历史记录

## V0.2
- 模板系统
- 风险识别
- 历史搜索

## V0.3
- SSH 收藏
- 语义搜索
- 项目上下文增强

## V0.4
- 注入当前终端
- 更强场景模板
- 准备商业化验证

---

## 18. 最终结论

这个 MVP 的本质不是“做一个新的终端”，而是：

**做一个能真正理解开发者终端任务的 AI 操作助手。**

MVP 设计必须坚持三点：

1. 先闭环，不求大而全
2. 先帮用户少查资料、少踩坑
3. 先建立信任，再考虑更深自动化

只要第一版能稳定解决“生成命令 + 看懂报错 + 复用经验”这三个问题，这个产品就已经有了继续迭代和商业化验证的基础。